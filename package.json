{
  "name": "rahvanounikud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch": "node scripts/fetch-feed.mjs",
    "parse": "node scripts/parse-songs.mjs",
    "build:data": "node scripts/build-data.mjs",
    "data": "npm run fetch && npm run parse && npm run build:data",
    "dev": "vite",
    "build": "npm run build:data && vite build && tsc --noEmit",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.1.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-router-dom": "^7.18.2"
  }
}
